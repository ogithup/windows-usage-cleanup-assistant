<Window x:Class="WindowsUsageCleanupAssistant.Views.CleanupProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Cleanup Progress"
        Height="520"
        Width="760"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock FontSize="24"
                   FontWeight="SemiBold"
                   Text="Cleanup in progress" />

        <StackPanel Grid.Row="1" Margin="0,16,0,0">
            <ProgressBar Height="18"
                         Maximum="100"
                         Value="{Binding PercentComplete}" />
            <StackPanel Margin="0,10,0,0" Orientation="Horizontal">
                <TextBlock FontWeight="SemiBold"
                           Text="{Binding PercentLabel}" />
                <TextBlock Margin="18,0,0,0"
                           Foreground="DimGray"
                           Text="{Binding StepLabel}" />
            </StackPanel>
            <TextBlock Margin="0,8,0,0"
                       FontWeight="SemiBold"
                       Text="{Binding CurrentStep}" />
            <TextBlock Margin="0,6,0,0"
                       Foreground="DimGray"
                       Text="{Binding CurrentItem}"
                       TextWrapping="Wrap" />
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,16,0,0" Orientation="Horizontal">
            <TextBlock FontWeight="SemiBold" Text="{Binding DeletedLabel}" />
            <TextBlock Margin="20,0,0,0" FontWeight="SemiBold" Text="{Binding SkippedLabel}" />
        </StackPanel>

        <Border Grid.Row="3"
                Margin="0,16,0,0"
                BorderBrush="#D8D8D8"
                BorderThickness="1"
                CornerRadius="12"
                Padding="8">
            <ListBox ItemsSource="{Binding ActivityLog}" />
        </Border>
    </Grid>
</Window>

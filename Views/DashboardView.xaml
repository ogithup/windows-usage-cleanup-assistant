<UserControl x:Class="WindowsUsageCleanupAssistant.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" MinHeight="260" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Row 0: Summary Cards -->
        <UniformGrid Grid.Row="0" Columns="5" Margin="0,0,0,20">
            <Border Style="{StaticResource SummaryCardStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource SummaryLabelStyle}" Text="Total Installed Apps" />
                    <TextBlock Style="{StaticResource SummaryValueStyle}" Text="{Binding TotalProgramCount}" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource SummaryCardStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource SummaryLabelStyle}" Text="Unused (180+ Days)" />
                    <TextBlock Style="{StaticResource SummaryValueStyle}" Text="{Binding UnusedProgramCount}" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource SummaryCardStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource SummaryLabelStyle}" Text="Cleanup Candidates" />
                    <TextBlock Style="{StaticResource SummaryValueStyle}" Text="{Binding CleanupCandidateCount}" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource SummaryCardStyle}">
                <StackPanel>
                    <TextBlock Style="{StaticResource SummaryLabelStyle}" Text="Dependency Risk" />
                    <TextBlock Style="{StaticResource SummaryValueStyle}" Text="{Binding DependencySensitiveCount}" />
                </StackPanel>
            </Border>
            <Border Style="{StaticResource SummaryCardStyle}" Margin="0">
                <StackPanel>
                    <TextBlock Style="{StaticResource SummaryLabelStyle}" Text="Reclaimable Space" />
                    <TextBlock Style="{StaticResource SummaryValueStyle}" Text="{Binding ReclaimableSpaceText}" />
                </StackPanel>
            </Border>
        </UniformGrid>

        <!-- Row 1: Middle lists (2-column panel) -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Column 0: Top 10 Largest Programs -->
            <Border Grid.Column="0" Style="{StaticResource PageCardStyle}" Margin="0,0,10,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Style="{StaticResource SectionTitleStyle}" Text="Largest Installed Programs (Top 10)" Margin="0,0,0,10" />
                    
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding TopPrograms}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#EBE6DC" BorderThickness="0,0,0,1" Padding="0,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock FontWeight="SemiBold" Text="{Binding DisplayName}" FontSize="14" Foreground="#1F2A30" />
                                                <WrapPanel Margin="0,4,0,0" VerticalAlignment="Center">
                                                    <TextBlock Foreground="#6B7B84" Text="{Binding Publisher}" FontSize="12" Margin="0,0,8,0" />
                                                    <TextBlock Foreground="#6B7B84" Text="|" FontSize="12" Margin="0,0,8,0" />
                                                    <TextBlock Foreground="#6B7B84" Text="{Binding CategoryLabel}" FontSize="12" Margin="0,0,8,0" />
                                                    <TextBlock Foreground="#6B7B84" Text="|" FontSize="12" Margin="0,0,8,0" />
                                                    <TextBlock FontWeight="SemiBold" Foreground="#17494D" Text="{Binding EstimatedSizeMB, StringFormat={}{0:N0} MB}" FontSize="12" />
                                                </WrapPanel>
                                            </StackPanel>
                                            <Border Grid.Column="1" VerticalAlignment="Center" Style="{StaticResource RecommendationBadgeBorderStyle}">
                                                <TextBlock Style="{StaticResource RecommendationBadgeTextStyle}" Text="{Binding Recommendation}" FontSize="11" />
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Column 1: Top 10 Recently Used Programs -->
            <Border Grid.Column="1" Style="{StaticResource PageCardStyle}" Margin="10,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" Style="{StaticResource SectionTitleStyle}" Text="Recently Used Programs (Top 10)" Margin="0,0,0,10" />
                    
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding RecentPrograms}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border BorderBrush="#EBE6DC" BorderThickness="0,0,0,1" Padding="0,8">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0">
                                                <TextBlock FontWeight="SemiBold" Text="{Binding DisplayName}" FontSize="14" Foreground="#1F2A30" />
                                                <WrapPanel Margin="0,4,0,0" VerticalAlignment="Center">
                                                    <TextBlock Foreground="#6B7B84" Text="Last Launch:" FontSize="12" Margin="0,0,4,0" />
                                                    <TextBlock FontWeight="SemiBold" Foreground="#17494D" Text="{Binding LastUsedUtc, StringFormat={}{0:yyyy-MM-dd HH:mm}}" FontSize="12" Margin="0,0,8,0" />
                                                    <TextBlock Foreground="#6B7B84" Text="|" FontSize="12" Margin="0,0,8,0" />
                                                    <TextBlock Foreground="#6B7B84" Text="Launches:" FontSize="12" Margin="0,0,4,0" />
                                                    <TextBlock FontWeight="SemiBold" Foreground="#17494D" Text="{Binding LaunchCount}" FontSize="12" />
                                                </WrapPanel>
                                            </StackPanel>
                                            <Border Grid.Column="1" VerticalAlignment="Center" Style="{StaticResource RiskBadgeBorderStyle}">
                                                <TextBlock Style="{StaticResource RiskBadgeTextStyle}" Text="{Binding SystemRiskScore, StringFormat={}Risk: {0}}" FontSize="11" />
                                            </Border>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>

        <!-- Row 2: Quick actions / Quick Filters -->
        <Border Grid.Row="2" Style="{StaticResource PageCardStyle}">
            <StackPanel>
                <TextBlock Style="{StaticResource SectionTitleStyle}" Text="Quick Filter Action Center" Margin="0,0,0,12" />
                <UniformGrid Columns="4">
                    <Button Style="{StaticResource QuickActionCardStyle}" 
                            Command="{Binding NavigateToFilterCommand}" 
                            CommandParameter="Unused">
                        <StackPanel>
                            <TextBlock Text="Unused Programs" FontWeight="Bold" FontSize="15" Foreground="#17494D" />
                            <TextBlock Text="Filter to apps not launched in over 180 days to reclaim space." 
                                       Foreground="#6B7B84" FontSize="12" TextWrapping="Wrap" Margin="0,4,0,0" />
                        </StackPanel>
                    </Button>
                    
                    <Button Style="{StaticResource QuickActionCardStyle}" 
                            Command="{Binding NavigateToFilterCommand}" 
                            CommandParameter="Large">
                        <StackPanel>
                            <TextBlock Text="Large Apps" FontWeight="Bold" FontSize="15" Foreground="#17494D" />
                            <TextBlock Text="Filter to large files and apps taking up more than 1 GB storage." 
                                       Foreground="#6B7B84" FontSize="12" TextWrapping="Wrap" Margin="0,4,0,0" />
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource QuickActionCardStyle}" 
                            Command="{Binding NavigateToFilterCommand}" 
                            CommandParameter="CleanupCandidates">
                        <StackPanel>
                            <TextBlock Text="Cleanup Candidates" FontWeight="Bold" FontSize="15" Foreground="#17494D" />
                            <TextBlock Text="Filter to apps safe to delete based on rules and engine." 
                                       Foreground="#6B7B84" FontSize="12" TextWrapping="Wrap" Margin="0,4,0,0" />
                        </StackPanel>
                    </Button>

                    <Button Style="{StaticResource QuickActionCardStyle}" 
                            Command="{Binding NavigateToFilterCommand}" 
                            CommandParameter="SystemSdk"
                            Margin="0">
                        <StackPanel>
                            <TextBlock Text="System &amp; Runtimes" FontWeight="Bold" FontSize="15" Foreground="#17494D" />
                            <TextBlock Text="Filter to development tools, SDKs, and driver utilities." 
                                       Foreground="#6B7B84" FontSize="12" TextWrapping="Wrap" Margin="0,4,0,0" />
                        </StackPanel>
                    </Button>
                </UniformGrid>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>

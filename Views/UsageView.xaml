<UserControl x:Class="WindowsUsageCleanupAssistant.Views.UsageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Margin="0,0,0,16">
            <Button DockPanel.Dock="Right"
                    Width="110"
                    Command="{Binding DataContext.RefreshUsageCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                    Content="Refresh" />
            <TextBlock VerticalAlignment="Center"
                       Foreground="{DynamicResource MaterialDesignBodyLight}"
                       Text="Observed processes sampled every 30 seconds and stored in SQLite." />
        </DockPanel>

        <DataGrid Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  ItemsSource="{Binding UsageRecords}"
                  RowHeaderWidth="0">
            <DataGrid.Columns>
                <DataGridTextColumn Width="1.1*" Binding="{Binding ProcessName}" Header="Process" />
                <DataGridTextColumn Width="2.3*" Binding="{Binding ExecutablePath}" Header="Executable Path" />
                <DataGridTextColumn Width="1*" Binding="{Binding FirstSeenUtc, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Header="First Seen" />
                <DataGridTextColumn Width="1*" Binding="{Binding LastSeenUtc, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" Header="Last Seen" />
                <DataGridTextColumn Width="0.8*" Binding="{Binding TotalObservedMinutes, StringFormat={}{0:N1}}" Header="Minutes" />
                <DataGridTextColumn Width="0.8*" Binding="{Binding LaunchCount}" Header="Launches" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="2"
                   Margin="0,12,0,0"
                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                   Text="{Binding StatusText}" />
    </Grid>
</UserControl>
